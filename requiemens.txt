# Dependencias de runtime (vacías - la librería solo usa módulos estándar)
# re es un built-in module (regex) - no se instala

# Dependencias opcionales para desarrollo y testing
pytest>=7.0
black
flake8
mypy